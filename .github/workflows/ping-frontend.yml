name: Keep Frontend Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping frontend
        run: curl -s https://deep-learning-project-2-3.onrender.com/ > /dev/null
